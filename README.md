# Flag Map

A small static web app for clicking countries on a world map and building a copyable list of flag emojis.

## Run

Open `index.html` in a browser, or serve the folder locally:

```sh
python3 -m http.server 5173
```

Then visit `http://localhost:5173`.
